---

- name: pull role
  hosts: all
  tasks:
    - command: ansible-galaxy install -r requirements.yml -p /etc/ansible/roles --force

- name: Setup
  hosts: all
  vars_files:
    - ./vars.yml
  roles:
    - role: gumgum-flask
  tags:
    - setup

- name: Deploy
  hosts: all
  vars_files:
    - ./vars.yml
  roles:
    - role: gumgum-flask
  tags:
    - deploy
